# Python
__pycache__/
.pytest_cache/
*.py[cod]
venv/
.venv/

# Node
node_modules/
dist/

# Secrets — real values only; the .example files are committed
.env
.env.*
!.env.example
*.pem
*.key

# Design source assets: 2.4 MB of screenshots and a .rar that nothing in the
# app imports. Kept on disk, out of git — see Lock/ in the working tree.
Lock/

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# local tooling state — ไม่ใช่โค้ดโปรเจค
.claude/
CLAUDE.md
