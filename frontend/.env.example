# ไม่ต้องตั้งก็ได้ — ถ้าไม่มี โค้ดจะใช้ /api ซึ่งเป็น same-origin
# (nginx proxy /api/ ไปที่ backend บนเครื่องเดียวกันอยู่แล้ว)
# ตั้งเฉพาะตอน dev ที่ยิงข้ามเครื่อง เช่นรัน vite ในเครื่องแต่ใช้ API ของเซิร์ฟเวอร์
VITE_API_BASE_URL=
